// 06. 'Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon' split the string at the comma and change it to an array.

//===========SOLUTION============ 

let bigMNC = "Facebook, Google, Microsoft, Apple, IBM, Oracle, Amazon";

let arrBigMNC = bigMNC.split(", ");

console.log(arrBigMNC);